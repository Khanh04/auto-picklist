# Railway Environment Configuration
# These variables will be automatically set by Railway
# Railway provides either DATABASE_URL or individual PG* variables

# Option 1: Database URL (preferred by Railway)
# DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: Individual PostgreSQL variables (backup)
# PGUSER=postgres_user
# PGHOST=postgres_host
# PGDATABASE=postgres_database
# PGPASSWORD=postgres_password
# PGPORT=5432

# Legacy format for compatibility (mapped automatically in scripts)
# DB_HOST=postgres_host
# DB_PORT=5432
# DB_NAME=postgres_database
# DB_USER=postgres_user
# DB_PASSWORD=postgres_password

# Application Configuration
NODE_ENV=production
PORT=3000

# Migration Settings
MIGRATE_FROM_LOCAL=false

# Optional: File upload settings
MAX_FILE_SIZE=10485760
UPLOADS_DIR=uploads

# Security (set these in Railway dashboard)
# SESSION_SECRET=your_secure_session_secret_here

# Local database connection (only needed if MIGRATE_FROM_LOCAL=true)
# LOCAL_DB_HOST=your_local_host
# LOCAL_DB_PORT=5432
# LOCAL_DB_NAME=autopicklist
# LOCAL_DB_USER=postgres
# LOCAL_DB_PASSWORD=your_local_password